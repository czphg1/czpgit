spring:
  profiles:
    active: dev
  http:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
    encoding:
      charset: UTF-8
      force: true
      enabled: true
  thymeleaf:
    mode: LEGACYHTML5 #解决标签没有结束符/时报错的问题